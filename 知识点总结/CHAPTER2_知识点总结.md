# Chapter 2 知识点总结：上下文工程

> 本笔记以 `book/chapter2.md` 为依据，结合 `chapter2/` 下的实验项目，梳理 Agent 在一次任务之内如何组织、缓存、注入和压缩上下文。第二章的重点不是让提示词“写得更花哨”，而是把上下文当作一个需要设计、维护和验证的信息系统。

> 2026-09-14 复核：对照[第二章正文](../book/chapter2.md)和[实验台账](EXPERIMENT_LEDGER.md)修订，重点区分协议适用范围、缓存条件与实验结论。本次仅核对已有材料，未重新运行模型实验。

## 1. 本章的核心命题

上下文工程（Context Engineering）研究的是：

```text
在每一个决策点，应该让 Agent 看到什么信息，
以什么结构看到，以及怎样在长期运行中保持信息有效。
```

第一章给出的 Agent 公式是：

```text
Agent = LLM + Context + Tools
```

第二章进一步强调：

```text
模型能力决定基础智力；上下文质量决定能力能否真正发挥。
```

一个中等模型配上准确、结构化、及时的上下文，可能胜过一个顶级模型在信息不足时的盲目探索。对 Coding Agent 来说，至少需要三类背景：

- **代码上下文**：目录结构、模块职责、数据结构和代码规范；
- **流程规范**：分支策略、提交规范、审查流程和 CI/CD 要求；
- **环境信息**：工作目录、操作系统、运行配置、测试环境和密钥管理方式。

上下文工程也是组织问题。架构决策、业务规则和操作经验如果只存在于个人记忆或私聊中，Agent 就无法获得它们。因此，对远程协作友好、公开透明、文档驱动的团队，通常也更适合使用 Agent。

## 2. 上下文在 API 中的基本结构

本节以书中的 Chat Completions 风格接口为例，核心输入是消息列表 `messages`。这里使用的四种角色如下；其他接口可能使用不同角色、内容块或输入/输出 item 结构，并不都完全相同：

| 角色 | 来源 | 作用 |
|---|---|---|
| `system` | 开发者 | 定义身份、规则、约束和工作流程 |
| `user` | 用户或 Harness | 提供用户请求，也可承载框架注入的动态状态 |
| `assistant` | 模型 | 保存模型之前的文本回复或工具调用请求 |
| `tool` | Agent 框架 | 保存工具执行结果，并通过调用 ID 与请求配对 |

工具定义不是第五种消息角色，而是请求顶层的 `tools` 字段。它向模型说明工具名称、用途、参数和约束。

因此，第一章所说的五类上下文，在 API 层可以写成：

```text
系统提示词  → system 消息
用户消息    → user 消息
模型回复    → assistant 消息
工具结果    → tool 消息
工具定义    → tools 字段
```

本章示例采用**显式历史管理**：每次请求由 Agent 框架重新提交当前需要的消息历史。一些服务也提供托管会话或先前响应引用，无须客户端重复传输全部内容。需要区分“谁保存与组装历史”和“模型实际获得什么上下文”；服务端保存历史不等于自动拥有无限长期记忆。

## 3. 工具调用与 Agent 核心循环

典型的工具调用过程如下：

```text
1. Harness 发送 system、user 和 tools
2. 模型返回 assistant(tool_calls)
3. Harness 解析参数并真正执行工具
4. Harness 追加 assistant(tool_calls) 和 tool(result)
5. Harness 把更新后的完整 messages 再次发给模型
6. 模型继续调用工具，或输出最终文本
```

模型只负责决定：

- 是否需要工具；
- 调用哪个工具；
- 使用什么参数；
- 得到结果后是否还要继续。

真正的网络请求、文件操作或代码执行由 Agent 框架调度本地或远端工具完成。多个工具没有数据依赖、写入冲突或业务顺序约束时，可以考虑并行执行；如果后一个工具的参数必须由前一个结果产生，就需要按依赖串行执行。

最简核心循环可以用以下伪代码概括，`call_model` 等是示意函数，不是可直接运行的完整程序：

```python
while True:
    response = call_model(messages, tools)
    messages.append(response.message)

    if not response.message.tool_calls:
        return response.message.content

    for call in response.message.tool_calls:
        result = execute_tool(call.name, call.arguments)
        messages.append({
            "role": "tool",
            "tool_call_id": call.id,
            "content": result,
        })
```

生产代码还必须设置最大迭代次数、超时、参数校验、错误处理和高风险操作护栏，避免无限循环或危险执行。

此外，应按所用 SDK/API 将响应转换成合法的历史消息、把工具结果序列化为支持的格式，并检查结束原因。“没有工具调用”不保证一定得到了有效答案，仍可能是截断、过滤或空响应。

## 4. 静态前缀与动态轨迹

一次 Agent 请求的上下文可以分为两部分：

```text
静态前缀
├─ System Prompt
└─ Tool Definitions

动态轨迹
├─ User Messages
├─ Assistant Messages / Tool Calls
└─ Tool Results
```

- 静态前缀在整个会话中应尽量保持不变；
- 动态轨迹随着对话、思考和工具调用不断追加；
- 动态状态通常放在末尾，避免无必要地改写前缀；
- 压缩主要针对不断膨胀的动态轨迹。

这套“**稳定前缀 + 追加式轨迹**”结构，是理解本章所有技术的主线。

这是缓存友好的布局原则，不是不可违反的正确性约束。权限、安全规则或事实发生变化时，必须更新；不能为了命中缓存继续使用过期信息。

## 5. 注意力机制：Query、Key、Value

在因果 Transformer 中，可以把注意力理解为当前位置从已有序列中提取信息。处理当前已知 token 的表示，是为了参与预测后续 token；不是先为尚未生成的词拿到 Query：

| 向量 | 直观含义 | 功能 |
|---|---|---|
| Query（Q） | 当前 token 在找什么 | 向前文发出查询 |
| Key（K） | 每个历史 token 的检索标签 | 与 Query 计算匹配分数 |
| Value（V） | 每个历史 token 携带的内容 | 按匹配权重被提取和汇总 |

简化后的计算过程是：

```text
当前 Query 与所有历史 Key 匹配
             ↓
       得到注意力权重
             ↓
按权重汇总所有历史 Value
             ↓
       形成当前表示
```

更准确的单头形式是 `softmax(QKᵀ / sqrt(d_k) + causal_mask) V`：先计算并缩放匹配分数，再用因果掩码屏蔽未来位置，经过 softmax 得到权重，最后汇总 V。

因果语言模型只能关注当前 token 及其前面的 token，不能看到未来内容，所以注意力热力图通常呈三角形。

本章注意力可视化实验还展示了三个现象：

- **Attention Sink**：序列开头的 token 可能吸收大量无处安放的注意力权重；
- **位置偏好**：模型通常更重视上下文开头和结尾，中间信息更容易被忽视；
- **思考与输出的三角结构**：思维链会回看先前思考和工具定义，最终回答又会参考思考过程。

这些现象依赖模型、层、注意力头与输入，不能泛化成每个位置和每个头都如此。注意力热力图可用于观察分布，但不能单凭高权重就证明某段内容对答案有因果贡献，也不能把图当成完整的推理解释。

## 6. Chat Template：从消息对象到 Token 流

API 接收的是结构化消息，模型真正处理的是线性 token 序列。**Chat Template** 负责把前者转换成后者：

```text
{"role": "system", "content": "..."}
{"role": "user", "content": "..."}
             ↓ Chat Template
<|im_start|>system ... <|im_end|>
<|im_start|>user ... <|im_end|>
```

不同模型家族使用不同的特殊 token 和模板格式。开发者通常不需要手写模板，但必须使用模型支持的标准 API 结构。

把工具结果伪装成普通用户文本，或者自行拼成 `USER: ... ASSISTANT: ...`，主要问题不是必然破坏缓存，而是偏离模型训练时使用的消息格式。这会导致角色边界混乱、工具结果被误解、思维链被错误清除或工具循环行为退化。

不同模型和接口版本对历史思考的协议不同：可能清理某些历史思考，也可能要求在工具循环中保留特定字段或带签名内容块。书中列举的厂商案例用于说明这种差异，不构成所有版本的兼容性清单。

因此，历史思考能否保留、怎样回传，不能凭经验猜测，实施时必须核对所用模型与接口版本的协议。

## 7. KV Cache 与 Prompt Cache

### 7.1 KV Cache

模型生成新 token 时，需要读取全部历史 token 的 K、V。KV Cache 把已经计算过的 K、V 保存下来，避免每生成一个 token 都重新计算整个前缀。

它不只是省掉历史 K、V 的线性投影，还避免每步重新执行历史位置的整段前向计算。对于本节讨论的标准全注意力模型，新位置的注意力仍需读取允许关注的历史缓存。因此：

- 上下文越长，解码仍会变慢；
- KV Cache 会消耗显存和内存带宽；
- 它改善计算量，但没有让长上下文变成零成本。

### 7.2 Prompt Cache

| 对比项 | KV Cache | Prompt Cache |
|---|---|---|
| 层级 | 模型单次推理内部 | 推理引擎或 API 服务层 |
| 范围 | 一次请求中的生成过程 | 多次请求之间 |
| 复用内容 | 已计算 token 的 K、V | 相同请求前缀对应的 KV 状态 |
| 目标 | 避免单次解码重复计算 | 减少跨请求的 prefill 延迟和费用 |

标准前缀 KV 复用要求相同模型与相关配置下，**可复用部分拥有一致的有效 token 前缀**。这是必要条件，但实际 Prompt Cache 是否命中还取决于服务端支持、最小长度、缓存分块、保留时间、路由与隔离策略等。

### 7.3 前缀失效规则

如果第 `k` 个 token 发生变化：

```text
第 1 ～ k-1 个 token：理论上仍可复用
第 k 个 token 及其之后：需要重新计算
```

这里假设模型、位置与其他输入保持一致；实际服务可能只复用完整缓存块，且不保证每次都保留了这些状态。

改动越靠前，失效范围越大。系统提示词中的一个动态时间戳、空格变化或工具顺序变化，都可能让后面的大段缓存失效。

## 8. KV Cache 友好的三条实践原则

本章最应该记住的三条规则是：

1. **系统提示词和工具定义确定后尽量不做无必要改动**。保持文本、顺序、配置和序列化结果稳定；规则与权限更新优先于缓存。
2. **动态信息追加到上下文末尾**。时间、用户状态、计数器和环境变化不要塞进系统提示词。
3. **使用模型的标准消息与工具协议**。不要随意把结构化消息改造成纯文本。

常见错误模式及影响如下：

| 错误模式 | 主要问题 | 推荐做法 |
|---|---|---|
| 系统提示词内注入当前时间 | 每次请求都改变前部 token | 末尾注入或按需调用时间工具 |
| 前缀内更新用户余额或配额 | 破坏大段缓存 | 用状态工具或末尾状态栏 |
| 动态调整工具定义顺序 | 工具 schema 后面的缓存失效 | 使用固定、确定性的顺序 |
| 简单滑动窗口删除旧消息 | 破坏前缀并遗忘关键工具结果 | 阈值触发的语义压缩或归档 |
| 把角色消息拼成普通文本 | 偏离训练格式，削弱工具能力 | 保持标准 role/content 结构 |

滑动窗口尤其危险：Agent 可能忘记之前已经读取过文件或调用过工具，继而反复执行同一操作并陷入循环。

## 9. 缓存是一项架构约束

Prompt Cache 不应只在系统完成后再做优化，而应在设计阶段决定上下文布局：

- 把跨会话可复用的公共内容放在前面；是否支持跨用户复用由服务端隔离策略决定，不能借此混用用户私有状态；
- 把用户、环境和运行模式相关的动态内容放在缓存边界之后；
- 避免让多个运行条件在静态前缀中形成组合爆炸；
- 希望子 Agent 复用父 Agent 前缀时，应对齐模型、工具、思考配置及有效 token 前缀；稳定序列化有帮助，但还必须确认服务允许这种缓存复用；
- 大型工具结果被替换为摘要后，应持久化相同的替换文本，避免恢复会话时产生不同字节流。

本章还介绍了研究前沿：把 KV Cache 看成模型在 prefill 阶段生成的“笔记”，未来可能支持局部编辑和缓存块组合。但这仍属于研究阶段，当前生产系统仍应遵循稳定前缀原则。

## 10. 系统提示词的设计方法

系统提示词像 Agent 的员工手册。判断它是否合格，可以问：

> 一个聪明但不了解业务的新员工，读完后是否知道应该按什么流程工作？

### 10.1 语气与风格

- 明确回答长度、专业程度和表达方式；
- 避免让 Agent 在失败时进行冗长的自我辩护；
- `MUST`、`NEVER` 等强约束只用于真正关键的规则，过度使用会稀释效果。

### 10.2 结构化表达

- Markdown 适合组织标题、列表、流程和说明；
- XML 标签适合标记机器可识别的语义边界；
- 二者可以组合：Markdown 管理人机可读结构，XML 标记精确的数据类别和来源。

### 10.3 流程优于规则堆砌

将零散规则整理成 SOP：

```text
验证输入 → 分类 → 预处理 → 执行 → 验证结果
```

每个阶段应说明：当前目标、进入条件、异常处理和下一步。结构混乱会让模型难以判断规则的优先级和依赖关系。

### 10.4 业务规则必须可执行

“根据情况选择合适方案”通常太模糊。规则应明确：

- 分类标准；
- 允许和禁止的边界；
- 阈值与计算公式；
- 例外情况；
- 冲突时的优先级。

业务规则由产品和领域负责人定义，工程师负责将其准确、清晰地编码进提示词。模型适合在明确框架内判断，不适合替团队发明关键业务政策。

### 10.5 Few-shot 示例

当风格、格式或边界难以用规则准确描述时，可提供少量高质量、覆盖边界情况的示例。对于模型已经稳定完成的任务，额外示例可能收益有限；应通过对照测试决定是否值得增加 token，而不是一概判断有用或浪费。

如果示例位于前部，尽量保持固定。动态检索示例可以提高相关性，也可以放在稳定前缀之后；只有改动前缀时，才会使该位置之后的可复用范围缩小。要权衡效果、长度与缓存。

## 11. 工具定义的上下文设计

工具定义不仅要包含函数签名，还应说明：

- 适用和禁止使用的场景；
- 参数含义、格式、枚举和具体例子；
- 常见错误与边界条件；
- 性能建议，如可以批量或并行调用；
- 与其他工具的前后依赖关系。

清晰描述有助于表达接口语义，但收益需要实际测量。书中曾报告删除描述后错误增加；当前实验 2-4 台账明确记录，冻结的复现实验没有复现历史百分比，因此不能写成“本地实验已证明显著增加”。

当工具数量很大时，可以使用渐进式工具发现：先暴露精简目录或搜索入口，再按需加载完整 schema。在支持相应协议的系统中，可将发现结果追加到轨迹，以减少前部改动。运行时仍需注册、校验并授权实际可执行工具；把函数说明写进普通文本，不会自动创造可调用接口。具体缓存效果取决于工具发现和 API 的实现。

## 12. 提示注入与上下文安全

提示注入的本质是：外部数据中混入伪装成指令的文本，诱导 Agent 违背原始规则。

常见入口包括：

- 用户直接输入；
- 网页正文或不可见元素；
- 邮件和文档；
- PDF 元数据；
- 图片 EXIF 信息；
- 被投毒的知识库、记忆或第三方 Skill。

PDF 元数据、EXIF 或隐藏元素只有在读取工具提取并送入上下文后，才会通过该路径影响模型；并不是文件含有这些内容就自动被模型看到。

Agent 比普通聊天机器人风险更高，因为它可以写文件、发邮件、调用外部系统或泄露数据。

上下文层的核心防御是区分“指令”和“数据”：

1. **来源标记**：用 `<external_content source="...">` 等结构包裹不可信内容；
2. **角色隔离**：严格使用 `system/user/assistant/tool` 传递不同来源；
3. **输入清洗**：过滤常见注入模式，但只能作为辅助措施；
4. **状态与记忆防投毒**：不能把未经验证的外部片段直接写入高信任区域；
5. **分层防御**：高风险工具还要配合权限限制、沙盒、参数校验和人工确认。

提示词警告不能彻底消除提示注入。上下文防御只是第一道防线，不能代替执行层安全控制。

## 13. Agent Skills：渐进式披露

当所有领域知识都塞入系统提示词时，会浪费 token，并用无关内容稀释注意力。Skills 的思路是：

```text
先展示能力目录 → 任务需要时加载核心说明 → 必要时再读取详细资料
```

典型 Skill 分为三层：

| 层级 | 内容 | 加载时机 |
|---|---|---|
| 元数据 | `name`、`description` | 启动时常驻，供模型路由 |
| 核心流程 | 完整 `SKILL.md` | Skill 被触发时加载 |
| 细则与资源 | 引用文档、脚本、模板 | 具体步骤需要时再读取 |

Skill 还可以携带脚本和模板，组织可复用的操作流程。但它不能自行授予新的工具、账号或文件权限；实际执行仍受运行环境、工具可用性和授权约束。

`description` 是路由条件，不应只是宽泛的宣传语。好的描述应包含：

- 什么时候必须使用；
- 什么时候不该使用；
- 容易误触发的反例；
- 足够区分相邻 Skill 的关键词和边界。

在上下文位置上要区分：

- Skill 元数据目录通常属于稳定前缀；
- 完整 Skill 指令在触发时通过 user 消息、工具结果或文件读取追加进轨迹；
- 首次加载仍然有 prefill 成本，之后才能随稳定历史复用缓存。

Skills 的价值是模块化、按需加载、独立测试、版本控制和共享。使用某个模型生态时，还应尽量遵循该模型厂商训练过的 Agent 交互约定。

## 14. Agent 状态栏：把隐式状态变成显式知识

模型擅长从上下文中检索已有事实，却不擅长每轮都从大量原始轨迹中重新计数、归纳和维护状态。状态栏由 Harness 提前计算并在上下文末尾注入结构化摘要，例如：

```xml
<agent_status>
Current State:
- phone_call invoked 3 times (Xfinity: 3/3 max)
- TODO: cancel plan (in_progress)
- current working directory: /project
</agent_status>
```

它的作用是：

- 让 Agent 知道任务做到哪一步；
- 防止忘记约束、重复调用工具或偏离目标；
- 把需要重复推理的结果转成可以直接检索的知识；
- 利用上下文末尾的位置优势，引导模型注意关键状态。

常见状态栏内容包括：

| 类型 | 例子 |
|---|---|
| 任务规划 | TODO、当前步骤、未解决事项 |
| 事件侧信道 | 时间戳、位置、事件间隔 |
| 工具统计 | 每个工具和目标对象的调用次数 |
| 环境状态 | 时间、工作目录、系统、Shell、Python 版本 |
| 错误诊断 | 错误类型、参数、调用栈和修复建议 |

### 状态栏的维护原则

- **能用代码确定性维护，就不要让 LLM 批量统计长历史**；
- 如果必须使用 LLM，适合逐条抽取，再由代码汇总；
- 状态栏是有损投影，通常不应立即删除全部原始上下文；
- 模型往往高度信任状态栏，因此其准确率和抗投毒能力必须作为生产指标。

### 两种更新方式

| 方式 | 优点 | 代价 | 适用情况 |
|---|---|---|---|
| 每轮替换旧状态 | 上下文整洁、只保留最新状态 | 旧状态位置之后的缓存失效 | 轨迹较短或状态消息很大 |
| 持久追加新状态 | 完全追加式，对缓存友好 | 陈旧状态累积并占 token | 状态更新频繁、轨迹较长 |

框架常借用一条 `user` 角色消息承载状态栏，但它并不代表真实用户输入，而是 API 协议层面的注入方式。

这不是对所有模型都通用的注入位置：部分模板把新 user 消息解释为新一轮对话，从而影响思考保留与工具配对，应按协议选择。状态栏提示“最多调用三次”也不是硬限制，真正的次数与权限约束应由执行层计数和阻止。

## 15. 上下文学习、腐化与压缩

本章采用了一个有用的工程视角：**不要假设模型每轮都会自动把长历史归纳成准确状态，必要时应提前提炼。** 这不意味着上下文学习在机制上仅等于检索；它也可以体现示例适应与推理，只是不保证会自动、可靠地完成全部统计和归纳。

模型容易回答“第 37 条记录是什么”，却未必能低成本、稳定地回答“全部 100 条记录一共有多少种状态”。如果每轮都重新统计，思考 token、延迟和错误率会随上下文增长。

因此，状态栏与压缩是同一思路的两面：

```text
状态栏：在原始轨迹之外，增加预先提炼的结论
压缩：用预先提炼的结论，替换臃肿的原始轨迹
```

压缩有两个目的：

1. 控制窗口长度、推理费用和延迟；
2. 提高信息密度，减少模型从噪声中重新提取结论的负担。

**上下文溢出**与**上下文腐化（Context Rot）**不同：

| 问题 | 含义 |
|---|---|
| 上下文溢出 | 窗口已经装不下 |
| 上下文腐化 | 窗口还装得下，但因长度、位置、干扰或冲突等因素，模型利用信息的质量下降 |

所以不能只在达到窗口上限时才关注上下文质量。

## 16. 六种压缩策略

本章实验比较了六种方案：

| 策略 | 做法 | 主要特点 |
|---|---|---|
| 无压缩 | 保留全部工具原文 | 保留细节，但长任务可能溢出；短任务未必需要压缩 |
| 个体摘要 | 每条工具结果分别总结 | 可控但碎片化、重复多 |
| 组合摘要 | 合并多条结果后统一总结 | 去重较好，但超长输入可能被截断 |
| 上下文感知压缩 | 结合当前问题和已有信息定向总结 | 有机会保留更相关的信息；效率与遗漏风险需实测 |
| 带引用的上下文感知 | 摘要中保留来源链接 | 可回溯，但 token 成本更高 |
| 自适应窗口化 | 接近阈值时批量压缩未处理结果 | 前期保留原文，减少频繁缓存失效 |

压缩与 KV Cache 并不矛盾。压缩发生在两次模型调用之间：

- 系统提示词和工具定义保持不变；
- Harness 替换动态轨迹中的大型工具结果；
- 替换点之前的缓存仍可复用，之后的缓存需要重建；
- 因此适合达到阈值后批量压缩，不适合每轮零碎压缩。

上述批量方式是常见权衡，不是硬性规则；遇到特别大的单次输出，也可以先做预算控制。带链接的摘要仍然有损，网页会变化、引用也可能失效；需要可复现证据时应保存获授权的原始快照或版本。

## 17. 生产级分层压缩

成熟系统通常组合多种策略：

1. **工具输出预算控制**：完整内容落盘，模型只看预览；
2. **直接删除噪声**：导航栏、重复日志等低价值内容无需摘要；
3. **API 层微压缩**：所用服务支持时，按协议清理或压缩指定历史内容；否则由 Harness 自行管理；
4. **归档式摘要**：逐轮保留结构化历史，维持逻辑脉络；
5. **全量压缩**：最后手段，并设置连续失败熔断器。

压缩时需要遵守四项原则：

- **信息价值不均匀**：决策和约束高于支撑证据，证据高于噪声；
- **语义完整性**：保留人物、时间、对象、因果和适用范围；
- **任务相关性**：同一原文针对不同任务应得到不同摘要；
- **压缩即理解**：高质量压缩需要语义能力，结果还应可检查、可追溯。

建议显式设置保留优先级：

```text
1. 架构决策和关键约束：不得丢失
2. 已修改文件与关键变更：完整保留
3. 验证状态：必须保留
4. 未完成 TODO 和回滚笔记：必须保留
5. 大型工具输出：可删除原文，保留结论或引用
```

同时保持协议完整：工具调用与结果的 ID 必须配对，不能因随意删除一端导致历史非法；未完成任务、权限边界和关键证据也不能只追求压缩率而丢失。

## 18. 可分解任务中，优先考虑隔离

压缩是在噪声进入主上下文后再做减法；子 Agent 隔离则让大量中间材料从一开始就不进入主上下文：

```text
主 Agent：下发自包含任务
    ↓
子 Agent：在独立上下文中搜索、读取和分析
    ↓
主 Agent：只接收结论、证据位置和必要摘要
```

这样可以减少主上下文污染和部分压缩需求。代价是子 Agent 看不到全部背景、交接摘要可能丢信息，并且子任务自身也消耗 token、延迟和协调成本。适合边界清楚的独立任务，不代表一定更省钱或优于共享上下文。任务描述必须明确、自包含，并规定输出格式和证据要求。

## 19. 本章实验与目录对应关系

| 实验 | 主题 | 相关目录 |
|---|---|---|
| 实验 2-1 | 本地小模型服务、工具调用与 ReAct | `local_llm_serving/` |
| 实验 2-2 | 注意力热力图与 Q/K/V | `attention_visualization/` |
| 实验 2-3 | KV Cache 错误模式与对比 | `kv-cache/` |
| 实验 2-4 | 提示工程消融 | `prompt-engineering/` |
| 实验 2-5 | 直接、间接和记忆提示注入 | `prompt-injection/` |
| 实验 2-6 | 用 Skills 从论文生成演示文稿 | `agent-skills-ppt/` |
| 实验 2-7 | 注意力视角验证状态栏 | `attention_visualization/` |
| 实验 2-8 | 时间戳、计数器、TODO、错误和环境状态 | `system-hint/` |
| 实验 2-9 | 六种上下文压缩策略 | `context-compression/` |

其中实验数字用于理解机制，不应脱离实验模型、任务、上下文预算和运行配置泛化为所有系统的固定结论。

### 19.1 阅读实验记录时的重要校正

根据本次读取的 `EXPERIMENT_LEDGER.md`，应区分正文历史观察与仓库冻结实验：

- **2-4 提示消融**：台账明确指出未复现历史百分比，不能把历史错误率当成本轮代码的必然效果。
- **2-5 提示注入**：冻结实验记录的攻击成功率均为零；这是该模型与样本上的结果，不证明系统对所有攻击安全。
- **2-7 状态栏**：在已有 3/3 调用轨迹后，无状态栏时有 2/3 次生成拒绝继续调用，带状态栏时为 3/3。这属于极小样本观察，不构成普遍效果保证。
- **2-8 系统提示状态**：台账记录未复现原先的部分历史数字，包含零收益或负向结果，应保留这些证据。
- **2-9 压缩**：比较来自特定任务与预算，不能推导某种压缩对所有任务最优。

台账中的 `passed` 表示规定的实验流程和验收门槛已完成，不意味着所有原文预期方向都得到验证。复现时还需检查相应项目的 `validation/latest.json` 和原始证据；本次文档复核不等于重新跑过实验。

## 20. 容易混淆的概念

| 概念 A | 概念 B | 区别 |
|---|---|---|
| 上下文工程 | 提示工程 | 前者管理全部输入信息；后者主要优化系统提示词等指令 |
| KV Cache | Prompt Cache | 前者是模型推理内部缓存；后者跨 API 请求复用相同前缀 |
| API 消息 | Chat Template | 前者是结构化对象；后者将其变成模型 token 流 |
| 工具定义 | 工具结果 | 在本节接口中定义放在 `tools`；结果以 `tool` 消息进入轨迹；其他协议可能使用内容块或 item |
| Skill | Tool | Skill 提供按需加载的知识、流程和资源；Tool 提供可执行动作 |
| 状态栏 | 对话摘要 | 状态栏通常是确定性维护的当前状态；摘要是对历史语义的压缩 |
| 上下文溢出 | 上下文腐化 | 前者是容量不足；后者是信息仍在但难以有效利用 |
| 追加 | 替换 | 追加不改变已有前缀；替换会使变动点后的缓存失效 |

## 21. 学完本章后应能回答的问题

> 参考答案补充于 2026-09-15。“原文依据”为短摘录，答案为归纳；实现细节与适用边界另作说明。来源链接定位到本次核对的原文行号。

### 21.1 显式历史管理为什么每轮重传消息？服务端会话管理有什么不同？

**参考答案：** 在本章示例中，每次模型调用是独立请求，客户端必须把当前任务所需的历史、工具请求和结果重新提供，才能保持连续性。服务端会话可以保存这些状态，客户端通过 ID 引用续接。无论哪种方式，真正影响模型判断的是本次可获得的上下文，而不是 Python 对象是否一直存在。

**原文依据：** “第二次请求包含了第一次的全部对话历史”。见[第二章·多轮交互](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter2.md#L219)。服务端引用的代码补充见[第一章项目的 `previous_response_id`](https://github.com/bojieli/ai-agent-book/blob/main/chapter1/search-codegen/agent.py#L115)。

### 21.2 `assistant(tool_calls)` 和 `tool` 消息如何通过 `tool_call_id` 配对？

**参考答案：** 助手消息里每个工具请求有独立的 `id`；对应结果消息将同一值写入 `tool_call_id`。例如请求 ID 是 `call_123`，结果也必须写 `tool_call_id="call_123"`。一次请求了两个工具，就分别返回两个关联结果。配对依据是 ID，不是工具名或返回顺序；同名工具也可以被调用多次。

**原文依据：** “模型据此知道哪个结果对应哪个调用。”见[第二章·工具结果关联](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter2.md#L221)。

### 21.3 为什么模型申请工具调用但不亲自执行工具？

**参考答案：** 模型输出名称和参数，真实网络请求、文件访问和代码执行发生在外部程序或服务中。这个分工让 Harness 能实施权限、校验、超时和审计。即使工具调用能力经过训练成为模型原生能力，训练内化的仍是选择和使用策略，工具实现仍由基础设施提供。

**原文依据：** “Agent 框架执行工具后，将结果以 tool 角色的消息送回给模型。”见[第二章·消息的四种角色](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter2.md#L44)。决策与执行的进一步说明见[第一章·实验 1-2](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter1.md#L216)。

### 21.4 Chat Template 对角色边界、思维链和工具调用有什么影响？

**参考答案：** Chat Template 把结构化消息转换为模型使用的 token 序列，用特定标记表达角色、消息边界和工具交互。错误地把工具结果包装成用户消息，可能改变模型对来源和对话阶段的理解，并影响某些模板对历史思考的保留。应使用对应接口要求的消息结构，按具体模型协议处理推理字段和内容块。

**原文依据：** “划分每条消息的边界和角色。”见[第二章·Chat Template](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter2.md#L479)；历史思考差异见[同节第 493 行](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter2.md#L493)。

### 21.5 KV Cache 与 Prompt Cache 分别解决什么问题？

**参考答案：** KV Cache 缓存已有位置的键和值，避免生成每个新 token 时重新计算整个历史前缀。Prompt Cache 在请求之间复用相同前缀的计算状态，减少重复 prefill。前者侧重单次生成，后者侧重跨请求复用。长上下文仍需存储、读取缓存；实际跨请求命中还受模型配置、缓存保留和服务端策略影响。

**原文依据：** “KV Cache 加速单次请求内的 token 生成，Prompt Cache 减少跨请求的重复计算成本。”见[第二章·两个层级的缓存](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter2.md#L529)。

### 21.6 为什么系统提示词中的动态时间戳代价很高？

**参考答案：** 时间每次变化，会改变时间戳位置及后续 token 的前缀依赖，缩小可复用的缓存范围。时间戳越靠前，影响的后续内容可能越多。可以把动态时间放在稳定前缀之后，或需要时读取时间工具。代价来自前缀改变，不是时间信息本身不该提供。

**原文依据：** “使 token 序列从时间戳所在位置开始不同”。见[第二章·实验 2-3：动态系统提示词](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter2.md#L515)。

### 21.7 为什么工具定义应保持固定顺序？

**参考答案：** 即使工具集合和描述不变，重排顺序也会改变实际输入 token，从首个变化处影响后续缓存。稳定排序能避免这类无必要的重算。确实需要新增、撤销或更新工具权限时，应优先保证正确性，再按接口能力采用延迟加载等方式控制成本。

**原文依据：** “改变顺序就会使 token 序列从首个顺序变化的位置开始不同”。见[第二章·实验 2-3：工具定义的动态排序](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter2.md#L519)。

### 21.8 为什么简单滑动窗口可能让 Agent 重复调用工具？

**参考答案：** 只保留最近若干条消息，会把早期的工具结果、完成状态和失败经验一并丢掉。模型再次需要这些信息时，可能重新读取、重复搜索，甚至重做已完成的动作。此外，删去前部消息也破坏了前缀一致性。更稳妥的管理要保存关键状态、语义摘要和证据引用，并保持工具调用与结果配对。

**原文依据：** “它可能丢失关键的工具调用结果。”见[第二章·实验 2-3：滑动窗口](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter2.md#L521)。

### 21.9 系统提示词怎样从规则堆砌改成流程驱动？

**参考答案：** 按任务执行顺序组织规则，为每个阶段写明目标、输入、判断条件、操作和异常出口。例如“验证文件 → 分类 → 预处理 → 执行 → 验证结果”，再把备份要求、错误处理等放进对应步骤。交叉规则应说明优先级，模糊业务条件应转成明确阈值或决策分支。

**原文依据：** “提供了清晰的标准操作流程（SOP）”。见[第二章·流程驱动 vs 规则堆砌](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter2.md#L587)，业务条件的细化见[系统提示词的内容](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter2.md#L612)。

### 21.10 怎样评估 Few-shot 示例是否值得加入，并安排其上下文位置？

**参考答案：** 用相同任务对比有无示例，观察答案质量、格式合规、边界错误、token 与延迟。示例对难以用文字规则表达的风格或复杂格式尤其有用。固定通用示例可以放在稳定前缀；按任务检索的示例可放在稳定前缀之后。是否保留取决于实际收益，而不是示例数量越多越好。

**原文依据：** “当期望的输出难以用规则精确描述时”。见[第二章·Few-shot 示例](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter2.md#L634)，位置与缓存讨论见[第 636 行](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter2.md#L636)。固定任务做对照、动态示例后置，是结合本章缓存原则给出的实践归纳。

### 21.11 提示注入为什么在拥有工具的 Agent 中尤其危险？

**参考答案：** 外部网页、邮件、文档中的恶意指令一旦被当作授权，就可能借助工具转化为删除文件、发送消息或泄露数据等真实操作。工具既扩大了输入来源，也扩大了错误的外部影响。应区分指令与数据，并在执行层实施权限、参数验证和高风险确认；仅加一句提示词警告不足以形成完整防护。

**原文依据：** “被注入的指令可能导致 Agent 执行文件删除、发送邮件、泄露隐私数据等不可逆的操作。”见[第二章·提示注入](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter2.md#L676)。

### 21.12 Skills 的三层渐进式披露分别是什么？

**参考答案：** 第一层是名称和描述等元数据，让模型知道有哪些能力、何时使用；第二层是选中后加载完整 `SKILL.md`，获取核心流程；第三层是按流程需要读取引用资料、脚本或模板。这样将少量路由信息与大量实施细节分开，控制初始上下文。具体加载消息位置由运行时协议决定，Skill 也不自行授予执行权限。

**原文依据：** “先给 Agent 看一份目录摘要，需要时再加载完整内容”。见[第二章·Skills](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter2.md#L718)。三层定义分别见[元数据](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter2.md#L722)、[核心流程](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter2.md#L726)和[细则](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter2.md#L730)。

### 21.13 状态栏为什么应尽量由代码而不是 LLM 维护？

**参考答案：** 调用次数、任务状态和配额等数据通常有确定来源，可以在事件发生时由代码增量更新，避免每轮从长历史重新统计。若原始材料是自然语言，可让模型逐条抽取候选事实，再由代码汇总和校验。状态栏只表达选定维度，应保留必要证据供回查，并由执行层实施硬限制。

**原文依据：** “能用代码算就用代码算”。见[第二章·状态栏维护经验](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter2.md#L831)，有损投影的说明见[第 833 行](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter2.md#L833)。

### 21.14 上下文腐化与上下文溢出有何区别？

**参考答案：** 溢出是容量问题：输入与所需预算超过接口可容纳范围。腐化是利用质量下降：信息仍在窗口中，但因长度、位置、干扰或矛盾而难以有效使用。增加窗口主要缓解容量问题；整理状态、去重、压缩或隔离，则着眼于信息质量。两种问题可能同时出现。

**原文依据：** “即使上下文窗口足够大，把所有原始信息堆在上下文里也不是最优选择。”见[第二章·为什么需要压缩](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter2.md#L925)，窗口与成本限制见[第 923 行](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter2.md#L923)。本题用这两个动机区分容量与利用效果。

### 21.15 上下文感知压缩可能带来什么收益，又可能遗漏什么信息？

**参考答案：** 它结合当前任务筛选、去重和整理资料，减少重复读取成本，让模型直接利用已知结论与信息缺口。风险是把暂时看似无关、后续却重要的内容丢掉，例如时间、条件、来源、早期决策理由或失败路径。压缩应保留关键约束、未完成事项和验证状态，并让证据可追溯。

**原文依据：** 「同样的内容在“查找创始人名单”和“了解个人背景”两个不同的任务下，应该产生不同的压缩结果」。见[第二章·任务相关性](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter2.md#L1010)。容易遗漏的信息见[架构设计启示](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter2.md#L1019)。

### 21.16 压缩为什么会牺牲部分缓存，又为什么仍然值得？

**参考答案：** 压缩通常在两次请求之间替换部分历史，替换处的 token 改变，之后的缓存不能按原前缀直接复用。它换来的收益是缩短后续输入、降低持续处理成本、提高信息密度并避免窗口溢出。应比较压缩调用与重建缓存的成本和后续节省量，再决定何时压缩；常用方式是到阈值后批量处理。

**原文依据：** “替换位置之后的缓存会失效，但之前的缓存仍然有效。”见[第二章·压缩与 KV Cache](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter2.md#L965)，权衡见[第 966 行](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter2.md#L966)。

### 21.17 什么场景适合用子 Agent 隔离代替主上下文压缩？

**参考答案：** 子任务边界清楚、中间材料很多、最终只需少量结论时适合隔离，例如在大代码库中定位一个函数。主 Agent 提供目标、范围和证据要求，子 Agent 独立探索，返回位置、结论和必要依据。若子任务高度依赖主任务细节，交接可能昂贵或有损；还要计算子 Agent 自身的费用与延迟。

**原文依据：** “任务描述必须自包含、目标明确”。见[第二章·子 Agent 上下文隔离](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter2.md#L1033)，具体代码搜索例子见[第 1031 行](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter2.md#L1031)。

如果能够脱离原文解释这些问题，并能根据一个真实 Agent 的消息轨迹指出静态前缀、动态状态、缓存失效点和可压缩内容，就掌握了第二章的主要知识。

## 22. 一句话复习

> 上下文工程不是把更多内容塞给模型，而是让正确的信息以稳定、结构化、可检索且可压缩的形式，在正确的时机进入 Agent 的上下文。

## 23. 复核记录（2026-09-14）

- 限定消息角色、工具结构和显式历史示例的协议范围。
- 补全注意力计算与 KV Cache 原理，区分理论可复用与服务端实际命中。
- 去除具体模型版本的未经本次核验的兼容性断言，明确实施时按接口协议确认。
- 修正工具描述消融与状态栏等实验的概括，保留仓库记录中的未复现和小样本结果。
- 补充 Skill 不授予权限、状态栏不替代硬约束、压缩保持调用配对等边界。
- 将“示例一定浪费”“上下文学习仅是检索”“隔离一定优于压缩”等绝对表述改成可检验的条件判断。
